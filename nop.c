#include "monty.h"

/**
 * nop - deletes the first nodes
 * @head: a node in a dlistint list
 * @line_number: line number passed file.
 *
 */

void nop(stack_t **head, unsigned int line_number)
{
	(void) head;
	(void) line_number;
}
